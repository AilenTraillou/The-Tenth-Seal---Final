using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Analytics;

public class analtycsstrei : MonoBehaviour {

    //public static void NivelMuerte()
    //{
    //    Analytics.CustomEvent("NivelMuerte", new Dictionary<string, object> {
    //        {"Muerte en nivel", Character.nivel}
    //    });
    //}

    //public static void QuienMeMato()
    //{
    //    Analytics.CustomEvent("QuienMeMato", new Dictionary<string, object> {
    //        {"Asesino", Character.asesino}
    //    });
    //}
}

/* 
 * 
 *  Scene scene;
 *  public static string nivel;

 * 
 * scene = SceneManager.GetActiveScene();
 * if (life==0) {
 *
        if(scene.name=="casa abandonada")
        {
            brazo.nivel = "casa abandonada";
            analtycsstrei.NivelMuerte();
        }

        if(scene.name=="segundo nivel")
        {
            brazo.nivel = "segundo nivel";
            analtycsstrei.NivelMuerte();
        }
      if(scene.name=="cementerio")
        {
            brazo.nivel = "cementerio";
            analtycsstrei.NivelMuerte();
        }

     

    }



    hacer esto en el personaje principal,preguntar cuando muere:
     en que nivel mori?, con lo de arriba se puede saber en que nivel mori.
     y le paso el strin a analytics




    public static sring Asesino;

    public void OnCollisionEnter(Collision col) {

     if (col.gameObject.layer == LayerMask.NameToLayer("Babosa"))
        {
           brazo.Asesino = "babosa"
        }
    }

    if (life==0) {
            
        analtycsstrei.QuienMeMato();
    } 

*/







