using System.Collections;
using System.Collections.Generic;
using UnityEngine;
//using UnityEngine.Advertisements;

public class propaganda: MonoBehaviour
{

    // Use this for initialization
    //void Start()
    //{
    //    Advertisement.Initialize("1469918", true); //valido los ADS el serial es especifico de cada usuario de uniti esta en: "windows, servicies, ads, advancer androidID".
    //}

    //public static IEnumerator ShowAd() //la funcion que ejecuto para llamar los ads
    //{
    //    while (!Advertisement.IsReady())
    //        yield return new WaitForEndOfFrame();
    //    Advertisement.Show();
    //}

    //llamas a la funcion con esto:  StartCoroutine(Ads.ShowAd());
}
